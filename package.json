{
  "name": "hoodplaka67-preisstudio",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": { "start": "node server.mjs", "test": "node --test tests/*.test.mjs", "build": "node --check server.mjs && node --check public/app.js && node --check local/core.mjs", "build:public": "node scripts/build-public-site.mjs" },
  "engines": { "node": ">=22" }
}
